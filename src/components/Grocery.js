export const Grocery = () => {
  return (
    <h2>
      Think like this is a separate large module, which will be fetched in
      network and loaded only when it's render
    </h2>
  );
};

export default Grocery;
